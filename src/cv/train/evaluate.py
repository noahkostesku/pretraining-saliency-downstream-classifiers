"""Accuracy evaluation helpers for validation and test."""


def evaluate_model(model=None, dataloader=None):
    """Evaluate a model and return scalar metrics."""
    _ = (model, dataloader)
    return None
