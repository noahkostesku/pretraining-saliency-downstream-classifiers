"""Occlusion map generation with a fixed masking protocol."""


def generate_occlusion_map(model=None, images=None):
    """Generate occlusion-based saliency maps."""
    _ = (model, images)
    return None
