"""Shared perturbation implementation for insertion and deletion."""


def run_insertion_deletion(model=None, image=None, saliency=None):
    """Run insertion and deletion evaluation for one image."""
    _ = (model, image, saliency)
    return None
